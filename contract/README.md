# 📑 TaxRouter: Smart Contract Overview

Этот каталог содержит исходный код смарт-контракта **TaxRouter**, предназначенного для выполнения торговых операций в EVM-совместимых сетях (преимущественно Binance Smart Chain) через **EVM_TERMINAL**.

## 📌 Назначение

**TaxRouter** — это кастомный роутер-прослойка, который агрегирует ликвидность протоколов PancakeSwap V2 и V3. Он спроектирован для минимизации транзакционных издержек и автоматизации сбора фиксированной комиссии с торгового оборота.

### Основные возможности:
- **Унифицированный интерфейс:** Позволяет совершать сделки в пулах V2 и V3 через один контракт.
- **Встроенная комиссия 0.1%:** Автоматическое удержание 10 BPS (basis points) с каждой сделки в пользу администратора.
- **Поддержка Quote-токенов:** Комиссия взимается только в ликвидных активах (USDT, WBNB и т.д.), исключая накопление неликвидных "щиткоинов" на балансе `fee_receiver`.
- **Support Fee-on-Transfer:** Корректная работа с дефляционными токенами (имеющими собственный налог на перевод).
- **Auto-Fuel Utility:** Функции для обмена нативных монет (BNB) на токены и обратно для обеспечения кошельков газом.

---

## ⚙️ Спецификации компиляции

Для корректного деплоя и верификации используйте следующие параметры:

- **Язык:** Solidity
- **Версия компилятора:** `0.8.33`
- **EVM Version:** `Shanghai` (важно для корректной работы PUSH0)
- **Оптимизация:** `Enabled` (200 runs)
- **Лицензия:** `MIT`

---

## 🚀 Инструкция по развертыванию (Deployment)

Контракт может быть развернут через **Remix**, **Hardhat** или **Foundry**.

### Аргументы конструктора:
При деплое необходимо передать 4 адреса (пример для BSC Mainnet):
1. `_factory`: `0xca143ce32fe78f1f7019d7d551a6402fc5350c73` (PancakeSwap V2 Factory)
2. `_WETH`: `0xbb4CdB9CBd36B01bD1cBaEBF2De08d9173bc095c` (WBNB)
3. `_feeReceiver`: Ваш адрес для сбора комиссий.
4. `_v3Router`: `0x1b81D678ffb9C0263b24A97847620C99d213eB14` (PancakeSwap V3 Smart Router)

### Последовательность действий после деплоя:
1. **Верификация:** Верифицируйте контракт в блокчейн-эксплорере.
2. **Настройка стейблкоинов:** Вызовите функцию `setQuoteToken(address_токена, true)` для основных активов если нужно:
   - USDT: `0x55d398326f99059fF775485246999027B3197955`
   - USDC: `0x8AC76a51cc950d9822D68b83fE1Ad97B32Cd580d`
   - ASTER, USD1 и др.
   *(WBNB добавляется в список Quote-токенов автоматически при деплое).*

---

## 🛠 Функции управления (Admin Functions)

Управление контрактом доступно только с адреса `owner`:

*   **`setFeeReceiver(address)`**: Изменение адреса для получения комиссионных сборов.
*   **`setQuoteToken(address, bool)`**: Включение/выключение режима Quote для конкретного токена. Если токен помечен как Quote, комиссия будет браться именно в нем.
*   **`transferOwnership(address)`**: Передача прав управления контрактом.
*   **`rescueTokens(address, uint)`**: Вывод любых токенов или BNB, случайно отправленных на адрес контракта.

---

## 🔍 Логика начисления комиссий

Контракт реализует "неинвазивное" списание комиссии 0.1%:

1.  **При покупке (Buy):**
    Контракт принимает Quote-токен от пользователя, вычитает 0.1% и отправляет остаток в пул DEX. Пользователь получает целевой актив.
2.  **При продаже (Sell):**
    Контракт совершает обмен всего объема токенов пользователя на Quote-токен, вычитает 0.1% из результата и отправляет чистую сумму пользователю.

**Важно:** Все проверки `amountOutMin` (защита от проскальзывания) внутри контракта производятся **после** вычета комиссии роутера, что предотвращает ошибки исполнения сделок.

---

## ⚠️ Disclaimer и технические особенности

Смарт-контракт **TaxRouter** является неотъемлемым компонентом торговой логики терминала. Но сам терминал не имеет инструментов для его деплоя в блокчейне. Исходный код контракта предоставлен в данном репозитории для обеспечения полной прозрачности и возможности самостоятельного аудита.

### Интеграция и развертывание:

1.  **Локальный исходный код:** Вы можете использовать контракт, уже развернутый в сети (адрес по умолчанию указан в коде), либо развернуть свою собственную версию. Это позволяет вам полностью контролировать логику отчислений или вовсе убрать их перед деплоем.
2.  **Связь с Rust-ядром:** Адрес используемого контракта жестко зафиксирован в конфигурации нативного ядра по адресу: `evm_terminal/rust_module/src/config.rs`. Это сделано намеренно для обеспечения максимальной производительности при формировании транзакций.
3.  **Необходимость пересборки:** Если вы решили использовать свой экземпляр контракта с новым адресом, вам **необходимо**:
    *   Отредактировать файл `config.rs`, указав ваш новый адрес.
    *   Выполнить полную пересборку Rust-модуля (`maturin develop --release`). Либо пересборку проекта целиком с помощью `build.py`
    *   Без пересборки изменения не вступят в силу, так как адрес контракта вшивается в бинарный файл во время компиляции.
4.  **Модификация логики:** Вы вольны изменять или удалять комиссионную составляющую (`FEE_BASIS_POINTS`) в Solidity-коде. Однако учитывайте, что структура входных параметров функций должна оставаться неизменной, иначе Rust-ядро не сможет корректно взаимодействовать с контрактом (возникнет ошибка `Execution Reverted`).
5.  **Отказ от ответственности:** Автор ПО не несет ответственности за любые финансовые потери. Код контракта предоставлен «как есть». Ошибки при самостоятельном деплое, неверная настройка адресов в `config.rs` или использование некорректных адресов роутеров DEX могут привести к потере средств. 

**Рекомендация:** Перед запуском в Mainnet протестируйте всю цепочку (деплой -> правка config.rs -> компиляция -> торговля) в сети **BSC Testnet**. Все необходимые адреса для тестов и публичные RPC указаны выше и в `config.rs`.